services:
  app:
    build: .
    container_name: guardini
    restart: unless-stopped
    shm_size: '1gb'
    environment:
      - PYTHONFAULTHANDLER=1
    ports:
      - "127.0.0.1:8504:8501"
    volumes:
      - guardini_runs:/data/runs

volumes:
  guardini_runs:
