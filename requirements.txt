# ============================================================
# requirements per il backend Guardini (FastAPI + engine)
# Usato dal Dockerfile (stage runtime).
#
# NB: la v9 Streamlit (guardini_j_galileo_v9.py, non deployata)
# richiede in più: streamlit==1.41.1 e plotly==5.24.1.
#
# pandas/pyarrow pinnati alle versioni testate; pyarrow serve
# anche per la persistenza parquet dei run.
# ============================================================

fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
statsforecast==2.0.3
openpyxl==3.1.5
xlsxwriter==3.2.2
pandas==2.2.3
pyarrow==18.1.0
